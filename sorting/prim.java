class prim {
  // distance of a fringe vertex from the tree
  static int[] d;
  // vertex connectin a fringe vertex to the tree vertex
  static int[] pi;
  // whether a vertex is in the minimum spanning tree being built
  static int[] S;
  // Priotity queue of fringe vertices in the order of the distances from the tree
  static PriorityQueue pq;

  static primMST(Graph G, int s, int n){
    pq = new PriorityQueue();
    // for each vertex v
  }
}
